import React from 'react';
import ReactDOM from 'react-dom';
import './styles/Header.sass';

const Header = () => {
    return(
        <header>
            <h1>Movie App</h1>
        </header>
    )
}

export default Header
